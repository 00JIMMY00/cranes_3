# Production Docker Compose - Cranes Management System
# Domain: cranes.wahet-elarab.com
# Note: Caddy reverse proxy should be handled in a separate compose file

version: '3.8'

services:
  # PostgreSQL Database
  db:
    image: postgres:16-alpine
    container_name: cranes_db
    restart: unless-stopped
    env_file:
      - .env.prod
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5446:5432" # expose DB externally
    networks:
      - cranes_network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s


  # Django Backend
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: cranes_backend
    restart: unless-stopped
    env_file:
      - .env.prod
    environment:
      - DEBUG=False
      - DB_NAME=${POSTGRES_DB}
      - DB_USER=${POSTGRES_USER}
      - DB_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    depends_on:
      db:
        condition: service_healthy
    networks:
      - cranes_network
      - reverse_proxy
    expose:
      - "8000"
    healthcheck:
      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/')" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # React/Vite Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - API_URL=${VITE_API_URL}
    container_name: cranes_frontend
    restart: unless-stopped
    volumes:
      - frontend_dist:/dist
    networks:
      - cranes_network
      - reverse_proxy

volumes:
  postgres_data:
    driver: local
  static_volume:
    name: cranes_3_static_volume
    driver: local
  media_volume:
    name: cranes_3_media_volume
    driver: local
  frontend_dist:
    name: cranes_3_frontend_dist
    driver: local

networks:
  cranes_network:
    driver: bridge
  reverse_proxy:
    external: true
